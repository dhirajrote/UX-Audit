# Copy this file to .env and fill in real values.
# Used by the /api serverless functions only — never exposed to the browser.

# Anthropic (powers the AI Assistant + per-issue Generate buttons)
ANTHROPIC_API_KEY=

# Supabase (Project Settings -> API)
SUPABASE_URL=https://wghlqbfxnfpfplkwoypp.supabase.co/rest/v1/
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6IndnaGxxYmZ4bmZwZnBsa3dveXBwIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MzI1NjYyMCwiZXhwIjoyMDk4ODMyNjIwfQ.5HWDiP_iNtKZzuClZWeI_0N_Gf15V5TqdoHtBg17I-w

# Login — username/password are hardcoded in api/auth.js (dheerajrote / Qwerty123!@#).
# Since this repo is public, that means they are NOT secret. Leave these two
# blank to use the hardcoded defaults, or set them here/in Vercel to override
# without touching code (overrides take priority over the hardcoded values).
AUTH_USERNAME=Dheerajrote
AUTH_PASSWORD=Qwerty123!@#
# Required. Any long random string — used to sign session cookies, and must
# stay private (never hardcode this one). Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET = 2f348936255d197248a31abb366e154d5fc0b6d3ce60c46fb2c4b5f1f1812443
